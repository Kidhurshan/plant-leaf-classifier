# ---------------------------------------------------------------------------
# EC8010/EC8020 Design Task 4 -- plant leaf species classification
# Assumed Python: 3.10-3.12 (Google Colab default is 3.11/3.12).
#
# IMPORTANT (Colab):
#   torch / torchvision are ALREADY installed on the Colab runtime and are
#   built against that runtime's exact CUDA version. We deliberately do NOT
#   reinstall them here -- forcing a different build is the #1 cause of broken
#   CUDA on Colab. The floors below are only asserted, not force-upgraded, by
#   the notebook install cell. If you run off-Colab, install a CUDA-matched
#   torch from https://pytorch.org first, then `pip install -r requirements.txt`.
# ---------------------------------------------------------------------------

# --- Deep learning (Colab-managed; floors only) ---
# torch>=2.3 for the torch.amp.GradScaler(device) / autocast(device_type=...) API.
torch>=2.3
torchvision>=0.18

# --- Models / backbones (pinned; provides all three timm backbones) ---
timm==1.0.15

# --- Dataset download ---
kaggle==1.6.17

# --- Numerics / metrics ---
numpy>=1.26,<3            # Colab-managed core (floor only, avoids ABI churn)
scikit-learn==1.5.2       # metrics: f1, confusion matrix, classification report, t-SNE

# --- Config ---
PyYAML==6.0.2

# --- Visualisation ---
matplotlib==3.9.2         # all figures (viz.py is matplotlib-only)

# --- Image IO (Colab-managed; floor only) ---
Pillow>=10.0

# --- Web demo (notebook 08): browser upload + 3-model comparison UI ---
gradio>=4.44,<6

